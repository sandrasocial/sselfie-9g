# Cross-repo sync policy

The **canonical** three-repo sync policy lives in **`/Users/MD760HA/agents-sselfie/SYNC.md`**.

**This repo:** `sselfie-9g` (mothership). **Do not** import `lib/maya/` from `agents-sselfie` or `sselfie-studio-v2`.

**Start here in this repo:** `AS-BUILT.md`, then `CLAUDE.md` for business context.

Run **`pnpm verify:repo`** after structural checks.
