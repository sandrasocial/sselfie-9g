import { HomePageContent } from "@/components/sselfie/public-marketing"

export default function LandingPageEducation({ referralCode }: { referralCode?: string | null }) {
  return <HomePageContent referralCode={referralCode} />
}
