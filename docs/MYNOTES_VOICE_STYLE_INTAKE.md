# MyNotes Voice + Style Intake (Required Inputs from Sandra)

## Purpose

This checklist defines exactly what we need from `MyNotes` so Codex, Claude, and Stella can generate brand-aligned content and actions without drifting from your voice.

## Step 1: Provide source location

Status: `Completed on 2026-02-12`

Current canonical source provided:

1. `/Users/MD760HA/Downloads/Sandra's Voice Bible.md`
2. Long-form story shared in Codex thread (same date).

If you want to add more sources, provide one of these:

1. Absolute path to `MyNotes` export/folder on your Mac.
2. A single markdown/doc file containing your voice/style notes.
3. A curated folder with only relevant notes.

If there are multiple note systems, pick one canonical source first.

## Step 2: Provide canonical voice inputs

Copy or link examples for each item:

1. `Best-performing long-form post` (3-5 examples).
2. `Best-performing story sequence` (3-5 examples).
3. `Best-performing DM response` (10-20 examples).
4. `Best-performing sales CTA` (5-10 examples).
5. `Emails that felt most "you"` (5 examples).

For each example include:

- channel,
- date (if known),
- audience/context,
- result (replies, sales, saves, etc. if known).

## Step 3: Define voice boundaries

Answer these explicitly:

1. Words/phrases you always use.
2. Words/phrases you never want used.
3. Tone defaults (for example: direct, warm, sharp, bold, calm).
4. Tone by context:
   - sales,
   - education,
   - support,
   - objection handling.
5. Red lines:
   - claims to avoid,
   - compliance boundaries,
   - topics to never discuss.

## Step 4: Define offer and audience truth

1. Core audience segments (top 3).
2. Top pains and desired outcomes by segment.
3. Offer ladder you want now:
   - freebie,
   - membership,
   - Brand Engine cohort,
   - Brand Engine VIP.
4. Pricing boundaries:
   - floor price,
   - target price,
   - max discount allowed.
5. Non-negotiable brand promise.

## Step 5: Approve output format for agents

Choose one:

1. `Draft-only mode` (recommended for now): agents draft, Sandra approves.
2. `Hybrid mode`: low-risk messages auto-send, sales copy approval-required.
3. `Auto mode`: only after 2+ weeks of successful QA.

## Acceptance criteria for completion

`MyNotes` intake is considered complete only when:

1. Source path is provided and readable.
2. Minimum examples are provided for each channel.
3. Do/Don't boundaries are explicitly listed.
4. Offer ladder and pricing boundaries are approved.
5. Mode (`Draft-only`, `Hybrid`, `Auto`) is selected.

## How progress will be monitored

1. Track completion in `docs/AI_PROGRESS_TRACKER.md` (`V-01`, `V-02`, `V-03`).
2. Store extracted rules in canonical docs:
   - `docs/brand/VOICE_BIBLE.md`
   - `docs/brand/DO_DONT.md`
   - `docs/brand/MESSAGING_PILLARS.md`
3. Weekly QA review:
   - voice match score,
   - approval rate,
   - conversion performance per content type.

## What I need from you now

1. Confirmation of `Draft-only mode` for this launch cycle.
2. Your floor and target pricing for:
   - Brand Engine cohort,
   - Brand Engine VIP.
3. Optional but high-value: 3-5 best-performing examples each for story, DM, and email with outcomes.
