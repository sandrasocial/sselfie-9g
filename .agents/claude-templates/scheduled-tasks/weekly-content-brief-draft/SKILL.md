---
name: weekly-content-brief-draft
description: Research and prepare SSELFIE's weekly content plan and Story themes from current evidence. Never posts. Keeps tutorials, story, and Sandra's build-in-public edge alive without forcing one message shape.
---

# Weekly Content Brief Draft

Working directory: `/Users/MD760HA/ACTIVE/sselfie-9g`.

Read `docs/brand/SSELFIE_BRAND_CONSTITUTION.md`,
`docs/business/SSELFIE_COMPANY_KERNEL_2026-07-16.md`, Source Of Truth, Voice OS,
`docs/strategy/CONTENT_INTELLIGENCE_SYSTEM_2026-07.md`, and
`scripts/weekly-brief-prep.ts`.

## Unattended safety

Use Read, WebSearch, and `npx tsx scripts/weekly-brief-prep.ts ...` only. Never send to customers,
publish, post, change an offer, or make production writes outside the approved storage/preview
script. Every number comes from the data command or a linked current source.
Private and unvalidated offers stay out of this unattended public task.

## Editorial job

Build a useful week for the capable, overwhelmed woman who is starting or restarting. Keep three
engines visible:

- tutorials create reach;
- Sandra's real story creates trust;
- building a real app and business with AI at 39 makes the future feel possible and distinct.

Use these as a flexible rhythm, not a quota inside every piece. A tutorial may simply teach. A story
may simply connect. A product invitation appears only when it is the honest next step.

## Run

1. Run `npx tsx scripts/weekly-brief-prep.ts data` for current Instagram, prompt, funnel, SUITE, and
   recent-plan evidence.
2. Research two or three current content or AI-photo mechanics relevant to the audience. Borrow a
   useful mechanic, never a competitor's identity or unverified claim.
3. Identify the strongest current signal, what it may mean, what remains unknown, and what should be
   repeated or tested. Do not treat old DMs, views, or competitor results as paid proof.
4. Prepare the canonical payload required by the script:
   - `demandMap` with the strongest signal, before, desired after, belief shift, current public
     bridge or no ask, and content warning;
   - two or three `trendRadar` items with a buyer-safe `vibePreset` where visual direction applies;
   - exactly five `contentPlan` items using the script's current format, stage, goal, and CTA enums;
   - seven `dailyStories` themes, one per weekday;
   - `researchNotes` and a short `emailSummary`.
5. Run a quiet truth and voice pass. The opening should belong to Sandra or the specific woman, not
   a generic creator template.
6. Pipe the JSON into `npx tsx scripts/weekly-brief-prep.ts draft` and stop after storage and Sandra's
   preview.

Report the strongest signal, the five titles, the tested mechanics, the week's story rhythm, and any
current public product bridge. If evidence is thin, say what is uncertain rather than filling the
week with generic content.
